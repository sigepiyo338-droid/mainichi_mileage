# Google Cloud Run デプロイ ＆ クラウド同期ガイド

このガイドでは、**Mainichi Mileage** を Google Cloud Run（無料枠）にデプロイし、PCとスマホでデータを自動同期して利用する手順をわかりやすく解説します。

---

## 💡 この構成の特徴
- **完全セルフホスト**: 自分専用のGoogle Cloudアカウントで動かすため、他人にデータを見られません。
- **完全無料枠内で運用可能**: Cloud Run（月200万リクエスト無料）＋ Firestore（保存1GB・読取5万回/日無料）で、個人利用なら追加料金は一切かかりません。
- **合言葉による保護**: 初回アクセス時に設定した合言葉を入力した端末（ブラウザ）だけが同期できます。
- **複数端末対応**: PCで記録した時間がスマホにも即時反映されます。

---

## 事前準備（所要時間：約3分）

1. **Google Cloud コンソール**（ [https://console.cloud.google.com/](https://console.cloud.google.com/) ）にGoogleアカウントでログインします。
2. 画面上部から **「新しいプロジェクト」** を作成します（プロジェクト名例: `mainichi-mileage-app`）。
3. 左上メニュー（≡）から **「Firestore」** を開きます。
4. **「Firestore ネイティブモードを選択」** をクリックします。
5. ロケーションに **`asia-northeast1 (東京)`** （またはお好みのリージョン）を選択し、**「データベースを作成」** をクリックします。
   > ※これだけでデータベース側の準備は完了です！テーブルやコレクションの作成はアプリが自動で行います。

---

## デプロイ手順（2つの方法から選べます）

### 方法A：GitHub連携（★ブラウザだけで完結・最もおすすめ）

PCに特別なツールをインストールせず、GitHubリポジトリとGoogle Cloudを連携してデプロイする方法です。

1. **GitHubに最新コードをプッシュ**:
   - 本プロジェクトの最新コード（`Dockerfile`, `server.js` など）をお使いのGitHubリポジトリ（`sigepiyo338-droid/mainichi_mileage`）にプッシュします。
2. **Cloud Run サービスの作成**:
   - Google Cloud コンソールで **「Cloud Run」** を検索して開きます。
   - **「サービスの作成」** ボタンをクリックします。
3. **デプロイ設定**:
   - **デプロイ元**: 「ソースリポジトリから継続的に新しいリビジョンをデプロイする」を選択し、**「CLOUD BUILD の設定」** をクリック。
   - GitHubアカウントを認証し、リポジトリ（`mainichi_mileage`）とブランチ（`main`）を選択。
   - **ビルドタイプ**: 「Dockerfile」を選択（ソースの場所は `/Dockerfile` のままでOK）し、「保存」。
4. **サービスの設定**:
   - **サービス名**: `mainichi-mileage`（任意）
   - **リージョン**: `asia-northeast1 (東京)`
   - **認証**: **「未認証の呼び出しを許可」** を選択（URLを知っているブラウザから直接開けるようにします。合言葉で保護されるため安全です）。
5. **環境変数の設定（重要：合言葉の設定）**:
   - 画面下の **「コンテナ、ボリューム、ネットワーキング、セキュリティ」** をクリックして展開します。
   - 「コンテナ」タブの **「環境変数」** に以下を追加します：
     - **名前**: `SYNC_PASSPHRASE`
     - **値**: `あなたが決めた合言葉（例: my-secret-2026）`
6. **作成の実行**:
   - 画面最下部の **「作成」** をクリックします。
   - 約2〜3分でビルドとデプロイが完了し、上部に **専用のURL**（例: `https://mainichi-mileage-xxxxxx.a.run.app`）が表示されます！

---

### 方法B：Google Cloud Shell を使う方法（コマンドで一発）

Google Cloud コンソール右上にある **「Cloud Shell をアクティブにする（ターミナルアイコン）」** を使ってデプロイする方法です。

1. Google Cloud コンソール右上の **「Cloud Shell」**（`>_` アイコン）をクリック。
2. 以下のコマンドを順に実行します：
   ```bash
   # リポジトリをクローン
   git clone https://github.com/sigepiyo338-droid/mainichi_mileage.git
   cd mainichi_mileage

   # Cloud Run にデプロイ（合言葉を設定）
   gcloud run deploy mainichi-mileage \
     --source . \
     --region asia-northeast1 \
     --allow-unauthenticated \
     --set-env-vars SYNC_PASSPHRASE="あなたが決めた合言葉"
   ```
3. 画面の指示に従い、API有効化のプロンプトが出たら `y` を入力します。
4. デプロイ完了後、発行された `Service URL` が表示されます。

---

## 端末（PC・スマホ）での使い方

1. **アクセス**:
   - 発行された Cloud Run のURLを、PCやスマートフォンのブラウザ（Safari、Chrome等）で開きます。
2. **合言葉の入力**:
   - 初回アクセス時、**「クラウド同期の合言葉」** 入力モーダルが自動的に表示されます。
   - デプロイ時に設定した合言葉（`SYNC_PASSPHRASE`）を入力し、**「認証して同期開始」** をクリックします。
3. **同期完了**:
   - ヘッダーに **「☁️ 同期完了」** バッジが表示されます。
   - 合言葉はこの端末のブラウザに記憶されるため、次回以降は自動で同期されます。
4. **スマホのホーム画面に追加**:
   - スマホのブラウザで「ホーム画面に追加」を行うと、全画面でネイティブアプリのように使えて便利です。

---

## トラブルシューティング

- **合言葉を忘れた・変更したい場合**:
  - Google Cloud Run のコンソールから「編集と新しいリビジョンのデプロイ」を開き、環境変数 `SYNC_PASSPHRASE` の値を書き換えて再デプロイしてください。
  - アプリ側の「設定」➔「合言葉を設定 / 変更」から新しい合言葉を再入力できます。
- **ローカルのファイル（`index.html`）を直接開いた場合**:
  - `file:///` で直接開いた場合、ヘッダーに「💻 ローカル」と表示され、従来のブラウザ内（localStorage）保存として正常に動作します。
